services:

  web:
    build:
      target: dev
    container_name: ${APP_NAME}_django_${APP_ENVIRONMENT}
    volumes:
      - ./../../../code/${APP_NAME}:/app
    entrypoint: [ "bash", "/app/entry_point.sh" ]
