services:

  web:
    build:
      context: .
      target: prod
      args:
        - PYTHON_VERSION=${PYTHON_VERSION}
        - CODE_REPO_URL=${CODE_REPO_URL}
    ports:
      - "${HTTP_ON_HOST}:8000"

